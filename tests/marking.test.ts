import { describe,it,expect } from 'vitest';
import { MarkingSchema, detectMissingCompulsorySentence, validateAllContentPoints } from '../src/lib/schema';
import { heuristicMark } from '../src/lib/marking';
const req={studentName:'Aina',part:'part3' as const,genre:'story' as const,question:'Write a story',expectedWordRange:'200-250',compulsorySentence:'I realised that time is more valuable than I thought.',contentPoints:[{id:'C1',requirement:'time'},{id:'C2',requirement:'lesson'}],essay:'I missed the bus and learned a lesson.'};
describe('marking safeguards',()=>{it('keeps criterion scores between 0 and 5',()=>{const m=heuristicMark(req); for(const c of [m.scores.content,m.scores.communicativeAchievement,m.scores.organisation,m.scores.language]) expect(c.score).toBeGreaterThanOrEqual(0), expect(c.score).toBeLessThanOrEqual(5)});it('total equals four criterion scores',()=>{const m=heuristicMark(req); expect(m.scores.total).toBe(m.scores.content.score+m.scores.communicativeAchievement.score+m.scores.organisation.score+m.scores.language.score)});it('detects missing compulsory story ending',()=>{expect(detectMissingCompulsorySentence(req.essay,req.compulsorySentence)).toBe(true)});it('evaluates all content points',()=>{const m=heuristicMark(req); expect(validateAllContentPoints(m,req.contentPoints)).toBe(true)});it('rejects malformed model output safely',()=>{expect(()=>MarkingSchema.parse({scores:{total:99}})).toThrow()})});
